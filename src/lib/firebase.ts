// Firebase has been removed. This stub exists so any stale imports don't crash.
export const firebaseAuth = null;
export const firebaseDB = null;
export const verifyFirebaseToken = async (_token: string) => null;
